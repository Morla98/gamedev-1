version: '3.0'
#
# Suggestion for port conventions:
#  - 80   : Frontend
#  - 80xx : Development (pgAdmin, ...)
#  - 90xx : Connectors (WebHooks)
#
services:
  # Main
  jiracollector:
    container_name: JiraCollector
    restart: always
    volumes:
      - ./config:/config
    build:
      context: .
    ports:
      - 9083:8080

networks:
  default:
    external:
      name: devgame-network
