package com.unihannover.gamedev.restcontroller;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.Ignore;
import org.junit.jupiter.api.Test;

/**
 * JUnit-Tests for package restcontroller.
 * Checks, if the methods in CollectorController work correctly.
 */
class CollectorControllerTest{

    /**
     * Tests, if a list of all Collectors in the repository is returned correctly.
     */
    @Test
    void testGetAllCollectors(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a list of all Collectors with given id in the repository is returned correctly.
     */
    @Test
    void testGetCollectorsById(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a Collectors is added to the repository correctly.
     */
    @Test
    void TestAddCollector(){
        //TODO
        assert(true);
    }
}