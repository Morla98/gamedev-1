package com.unihannover.gamedev.restcontroller;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.Ignore;
import org.junit.jupiter.api.Test;

/**
 * JUnit-Tests for package restcontroller.
 * Checks, if the methods in AchievementController work correctly.
 */
class AchievementControllerTest{

    /**
     * Tests, if the Achievments can be read out of the repository correctly.
     */
    @Test
    void testGetAllAchievements(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if the Achievments of a collector can be read out of the repository correctly.
     */
    @Test
    void testGetAchievementByCollectorId(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if an Achievments is added to the repository correctly.
     */
    @Test
    void testAddAchievementList(){
        //TODO
        assert(true);
    }
}