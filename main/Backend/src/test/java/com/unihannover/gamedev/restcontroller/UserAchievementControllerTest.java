package com.unihannover.gamedev.restcontroller;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.Ignore;
import org.junit.jupiter.api.Test;

/**
 * JUnit-Tests for package restcontroller.
 * Checks, if the methods in UserAchievementController work correctly.
 */
class UserAchievementController {

    /**
     * Tests, if a list of all Users in the repository is returned correctly.
     */
    @Test
    void testGetAllUserss(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a given User in the repository is returned correctly.
     */
    @Test
    void testGetUserAchievementsByUserEmail(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a preview of UserAchievements in the repository is returned correctly.
     */
    @Test
    void testGetUserAchievementsPreview(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a preview of UserAchievements with given collector id in the repository is returned correctly.
     */
    @Test
    void testGetUserAchievementsCollectorPreview(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a UserAchievement is added to or updated in the repository correctly.
     */
    @Test
    void testUpdateUserAchievements(){
        //TODO
        assert(true);
    }
}