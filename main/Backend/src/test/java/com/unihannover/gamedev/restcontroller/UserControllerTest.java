package com.unihannover.gamedev.restcontroller;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.Ignore;
import org.junit.jupiter.api.Test;

/**
 * JUnit-Tests for package restcontroller.
 * Checks, if the methods in UserController work correctly.
 */
class UserController {

    /**
     * Tests, if a list of all Users in the repository is returned correctly.
     */
    @Test
    void testGetAllUsers(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a given User in the repository is returned correctly.
     */
    @Test
    void testGetUsersByEmail(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if the information of a User in the repository is updated correctly.
     */
    @Test
    void testUpdateProfile(){
        //TODO
        assert(true);
    }

    /**
     * Tests, if a User is added to the repositry correctly.
     */
    @Test
    void testAddUser(){
        //TODO
        assert(true);
    }

}